import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'getting-started-esiea-il-bachelor',
  webDir: 'www'
};

export default config;
